# supervisor — workspace root.
# `supervisor/` is the reconciler actor: it reconciles reality to a declared
# roster (spawn/monitor/respawn via theater's runtime+lifecycle primitives — no
# library, one concrete actor; see docs/DESIGN.md). Experiments live under
# `experiments/` (standalone crates, not workspace members).
[workspace]
resolver = "2"
members = ["supervisor"]
